#include <gtest/gtest.h>

using namespace xitren;

/**
 * Constructs a new interval_event object 0.
 *
 * @short function The function to be called periodically
 * @unique_tag UNIQUE_ERROR_
 * @module main_module
 * @report ERROR
 * @because wait_between_checks The interval between checks for whether the function should be called, in
 * milliseconds.
 * @long function The function to be called periodically.
 * @doc_at function The function to be called periodically.
 * @solution1 function The function to be called periodically.
 * @solution2 function The function to be called periodically.
 * @solution3 function The function to be called periodically.
 */
TEST(base_test, base)
{
    problems.create_problem("Simple problem example", problem::error_type::error).add_because("Because");
}

/**
 * Constructs a new interval_event object 1.
 *
 * @short function The function to be called periodically
 * @unique_tag UNIQUE_ERROR_
 * @module main_module
 * @report ERROR
 * @because wait_between_checks The interval between checks for whether the function should be called, in
 * milliseconds.
 * @long function The function to be called periodically.
 * @doc_at function The function to be called periodically.
 * @solution1 function The function to be called periodically.
 * @solution2 function The function to be called periodically.
 * @solution3 function The function to be called periodically.
 */
TEST(base_test, base)
{
    problems.create_problem("Simple problem example", problem::error_type::error).add_because("Because");
}

/**
 * Constructs a new interval_event object 2.
 *
 * @short function The function to be called periodically
 * @unique_tag UNIQUE_ERROR_
 * @module main_module
 * @report ERROR
 * @because wait_between_checks The interval between checks for whether the function should be called, in
 * milliseconds.
 * @long function The function to be called periodically.
 * @doc_at function The function to be called periodically.
 * @solution1 function The function to be called periodically.
 * @solution2 function The function to be called periodically.
 * @solution3 function The function to be called periodically.
 */
TEST(base_test, base)
{
    problems.create_problem("Simple problem example", problem::error_type::error).add_because("Because");
}

/**
 * Constructs a new interval_event object 3.
 *
 * @short function The function to be called periodically
 * @unique_tag UNIQUE_ERROR_
 * @module main_module
 * @report ERROR
 * @because wait_between_checks The interval between checks for whether the function should be called, in
 * milliseconds.
 * @long function The function to be called periodically.
 * @doc_at function The function to be called periodically.
 * @solution1 function The function to be called periodically.
 * @solution2 function The function to be called periodically.
 * @solution3 function The function to be called periodically.
 */
TEST(base_test, base)
{
    problems.create_problem("Simple problem example", problem::error_type::error).add_because("Because");
}

/**
 * Constructs a new interval_event object 4.
 *
 * @short function The function to be called periodically
 * @unique_tag UNIQUE_ERROR_
 * @module main_module
 * @report ERROR
 * @because wait_between_checks The interval between checks for whether the function should be called, in
 * milliseconds.
 * @long function The function to be called periodically.
 * @doc_at function The function to be called periodically.
 * @solution1 function The function to be called periodically.
 * @solution2 function The function to be called periodically.
 * @solution3 function The function to be called periodically.
 */
TEST(base_test, base)
{
    problems.create_problem("Simple problem example", problem::error_type::error).add_because("Because");
}

/**
 * Constructs a new interval_event object 5.
 *
 * @short function The function to be called periodically
 * @unique_tag UNIQUE_ERROR_
 * @module main_module
 * @report ERROR
 * @because wait_between_checks The interval between checks for whether the function should be called, in
 * milliseconds.
 * @long function The function to be called periodically.
 * @doc_at function The function to be called periodically.
 * @solution1 function The function to be called periodically.
 * @solution2 function The function to be called periodically.
 * @solution3 function The function to be called periodically.
 */
TEST(base_test, base)
{
    problems.create_problem("Simple problem example", problem::error_type::error).add_because("Because");
}

